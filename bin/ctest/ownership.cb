class UserList {
    public {
        # Variable Declarations
        str name = ""
        int age = 0
        def getName(): str { ret *name }
    }
}
def main(): int {
    try readln("Name: " >> *UserList.name)
    println!(UserList.getName())
}