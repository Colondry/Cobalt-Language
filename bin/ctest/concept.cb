use! std::csm

class Shop {
    public {
        int value = 120
    }
}
def main() {
    new a = Shop
    new b = Shop

    b.value /= 2 # 50% Discount
    println!("a = {}, b = {}.", *a.value, *b.value) # prints 'a = 120. b = 60.'

    ret 0
}